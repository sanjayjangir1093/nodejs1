curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash
source ~/.bashrc
nvm install --lts (Install the latest LTS version of Node.js using nvm)
node -e "console.log('Running Node.js ' + process.version)" (Test that Node.js is installed and running correctly)
after create directory,git clone the repositroy and allow to port number 
